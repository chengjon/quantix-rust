---
phase: 03-real-live-broker-execution-closure
plan: 03
title: minimal safety constraints and verification flow
type: execute
wave: 3
requires: [LIV-03, LIV-02]
requirements_addressed: [LIV-03, LIV-02]
depends_on: [03-01, 03-02]
files_modified:
  - README.md
  - docs/USER_MANUAL.md
  - tests/repo_hygiene_test.rs
  - src/cli/handlers/tests/mod.rs
autonomous: true
user_setup: []
must_haves:
  truths:
    - docs and CLI teach the same minimum safe workflow for real submit
    - operators can see the preconditions, confirmation semantics, and post-submit verification step for `qmt_live`
  artifacts:
    - path: README.md
      provides: repo-level real-submit safety boundary wording
    - path: docs/USER_MANUAL.md
      provides: operator-facing verification flow for `qmt_live`
    - path: tests/repo_hygiene_test.rs
      provides: wording locks for the safety workflow
  key_links:
    - from: tests/repo_hygiene_test.rs
      to: README.md
      via: exact real-submit boundary and verification wording locks
    - from: tests/repo_hygiene_test.rs
      to: docs/USER_MANUAL.md
      via: exact `qmt_live` safety precondition wording locks
---

# Phase 3 Plan 03: Minimal Safety Constraints And Verification Flow

## Objective
- **What:** Lock the minimum safety constraints and operator verification flow for the guarded `qmt_live` real-submit path.
- **Why:** `LIV-03` is only complete when the repo teaches and tests the same safe workflow the code intends to enforce.
- **Output:** Updated docs, hygiene locks, and CLI-output regressions that formalize the real-submit verification loop.

## Context
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-real-live-broker-execution-closure/03-CONTEXT.md
@.planning/phases/03-real-live-broker-execution-closure/03-RESEARCH.md
@.planning/phases/03-real-live-broker-execution-closure/03-VALIDATION.md
@.planning/phases/03-real-live-broker-execution-closure/03-01-PLAN.md
@.planning/phases/03-real-live-broker-execution-closure/03-02-PLAN.md

## Tasks

<task id="03-03-01" type="docs">
  <name>Document the minimum qmt_live safety workflow</name>
  <files>README.md, docs/USER_MANUAL.md</files>
  <read_first>
    <file>README.md</file>
    <file>docs/USER_MANUAL.md</file>
    <file>src/cli/handlers/mod.rs</file>
    <file>src/execution/qmt_live_gate.rs</file>
    <file>.planning/phases/03-real-live-broker-execution-closure/03-RESEARCH.md</file>
  </read_first>
  <action>Revise the real-submit sections in README.md and docs/USER_MANUAL.md so they explicitly document the minimum safe workflow: `qmt_live` is the guarded path, bridge must advertise the required live capability state, preview-only is not real submit, generic `target_mode=live` remains unsupported, manual bridge execution requires deliberate confirmation, and post-submit broker verification should follow the printed `qmt-query` path or equivalent order-status inspection.</action>
  <acceptance_criteria>
    <item>`README.md` explicitly states the preconditions and boundary for real QMT submit.</item>
    <item>`docs/USER_MANUAL.md` explicitly states the preconditions, confirmation step, and follow-up verification step for `qmt_live`.</item>
    <item>No updated doc section claims that preview-only or generic `live` is sufficient for safe real submission.</item>
  </acceptance_criteria>
  <verify>rg -n "qmt_live|preview_only|target_mode=live|qmt-query|确认|live" README.md docs/USER_MANUAL.md</verify>
  <done>The primary user-facing docs teach a minimal safe real-submit workflow instead of only listing capabilities.</done>
</task>

<task id="03-03-02" type="test">
  <name>Lock the safety workflow with repo hygiene and handler regressions</name>
  <files>tests/repo_hygiene_test.rs, src/cli/handlers/tests/mod.rs</files>
  <read_first>
    <file>tests/repo_hygiene_test.rs</file>
    <file>src/cli/handlers/tests/mod.rs</file>
    <file>README.md</file>
    <file>docs/USER_MANUAL.md</file>
    <file>src/cli/handlers/mod.rs</file>
  </read_first>
  <action>Add or update regressions so docs and handler output fail if the minimum safety workflow drifts. Assert exact wording for the guarded `qmt_live` path, bridge/live preconditions, explicit confirmation semantics where applicable, and the broker-side follow-up verification command or equivalent guidance.</action>
  <acceptance_criteria>
    <item>`tests/repo_hygiene_test.rs` contains assertions for the real-submit boundary and verification workflow wording.</item>
    <item>`src/cli/handlers/tests/mod.rs` fails if the manual `qmt_live` path stops surfacing the post-submit verification guidance.</item>
    <item>`env RUSTFLAGS=-Clink-arg=-fuse-ld=bfd cargo test --test repo_hygiene_test` exits 0.</item>
  </acceptance_criteria>
  <verify>env RUSTFLAGS=-Clink-arg=-fuse-ld=bfd cargo test --test repo_hygiene_test</verify>
  <done>The safety workflow becomes a repo-level contract rather than an operator-memory convention.</done>
</task>

## Verification
- Run `env RUSTFLAGS=-Clink-arg=-fuse-ld=bfd cargo test --test repo_hygiene_test`.
- Re-run any added handler regressions for manual `qmt_live` output.
- Manually read the updated README and USER_MANUAL real-submit sections.

## Success Criteria
- [ ] Docs state the `qmt_live` preconditions and generic `live` boundary explicitly.
- [ ] Docs and handler output both teach the same post-submit verification step.
- [ ] The minimum real-submit workflow is locked in tests instead of remaining implicit.
