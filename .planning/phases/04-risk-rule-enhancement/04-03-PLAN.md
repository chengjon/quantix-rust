---
phase: 04-risk-rule-enhancement
plan: 03
title: auto reduce workflow and operator contract
type: execute
wave: 3
requires: [RSK-01]
requirements_addressed: [RSK-01]
depends_on: [04-01, 04-02]
files_modified:
  - src/risk/service.rs
  - src/cli/handlers/risk.rs
  - tests/risk_service_test.rs
  - tests/repo_hygiene_test.rs
  - README.md
  - docs/USER_MANUAL.md
autonomous: true
user_setup: []
must_haves:
  truths:
    - `auto-reduce` needs explicit semantics before operators can trust it
    - docs and CLI must not imply fully automatic selling unless that path is actually delivered
  artifacts:
    - path: src/risk/service.rs
      provides: auto-reduce trigger semantics
    - path: src/cli/handlers/risk.rs
      provides: operator-visible auto-reduce workflow output
    - path: tests/repo_hygiene_test.rs
      provides: docs/CLI wording locks for the chosen workflow
---

# Phase 4 Plan 03: Auto Reduce Workflow And Operator Contract

## Objective
- **What:** Turn `auto-reduce` from latent rule scaffolding into a real, bounded operator-visible workflow.
- **Why:** `RSK-01` remains incomplete until auto-reduce has explicit behavior rather than just a rule type and trigger helper.
- **Output:** Test-backed auto-reduce semantics plus aligned CLI/doc wording.

## Tasks

<task id="04-03-01" type="design">
  <name>Choose the minimal safe auto-reduce behavior contract</name>
  <files>src/risk/service.rs, src/cli/handlers/risk.rs, README.md, docs/USER_MANUAL.md</files>
  <action>Define whether v1 auto-reduce is recommendation-only, audit-only, or explicit operator-assisted execution. Prefer the narrowest behavior that closes the requirement without silently broadening automated execution authority.</action>
  <acceptance_criteria>
    <item>The chosen behavior is explicit in code, docs, and operator output.</item>
    <item>No surface implies automatic sell execution unless it is truly implemented.</item>
  </acceptance_criteria>
</task>

<task id="04-03-02" type="test">
  <name>Lock auto-reduce trigger semantics and operator wording</name>
  <files>tests/risk_service_test.rs, tests/repo_hygiene_test.rs</files>
  <action>Add regressions for trigger/no-trigger behavior and for the operator-facing wording that explains what the system does when auto-reduce conditions are met.</action>
  <acceptance_criteria>
    <item>Tests fail if auto-reduce remains a silent latent helper.</item>
    <item>Docs and CLI wording stay aligned with the delivered behavior.</item>
    <item>`env RUSTFLAGS=-Clink-arg=-fuse-ld=bfd cargo test --test risk_service_test --test repo_hygiene_test` exits 0.</item>
  </acceptance_criteria>
  <verify>env RUSTFLAGS=-Clink-arg=-fuse-ld=bfd cargo test --test risk_service_test --test repo_hygiene_test</verify>
</task>

## Success Criteria
- [ ] `auto-reduce` has explicit v1 semantics.
- [ ] Operator/docs wording matches the delivered behavior.
- [ ] `RSK-01` no longer depends on latent placeholder behavior.
