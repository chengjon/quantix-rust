---
phase: 04-risk-rule-enhancement
plan: 01
title: phase 4 baseline and current risk contract lock
type: execute
wave: 1
requires: [RSK-01]
requirements_addressed: [RSK-01]
depends_on: []
files_modified:
  - .planning/phases/04-risk-rule-enhancement/04-RESEARCH.md
  - .planning/phases/04-risk-rule-enhancement/04-VALIDATION.md
  - README.md
  - docs/USER_MANUAL.md
  - tests/repo_hygiene_test.rs
  - src/cli/tests/risk.rs
autonomous: true
user_setup: []
must_haves:
  truths:
    - `live_import`, `volatility-limit`, and `industry-blocklist` are already real parts of the repo
    - `industry-limit` and `auto-reduce` are not yet fully delivered and must remain explicitly bounded
  artifacts:
    - path: README.md
      provides: repo-level risk enhancement baseline wording
    - path: docs/USER_MANUAL.md
      provides: operator-facing current risk contract wording
    - path: tests/repo_hygiene_test.rs
      provides: wording locks for the current delivered risk boundary
  key_links:
    - from: tests/repo_hygiene_test.rs
      to: README.md
      via: exact risk-boundary wording locks
    - from: src/cli/tests/risk.rs
      to: src/cli/handlers/risk.rs
      via: current risk command contract
---

# Phase 4 Plan 01: Phase 4 Baseline And Current Risk Contract Lock

## Objective
- **What:** Lock the current Phase 4 baseline so the planning state reflects what risk features already exist versus what is still deferred.
- **Why:** Phase 4 cannot be planned coherently while delivered `live_import` / `volatility-limit` / `industry-blocklist` behavior is mixed together with placeholder `industry-limit` and deferred `auto-reduce`.
- **Output:** Research-backed baseline docs/tests that separate shipped risk behavior from the remaining implementation gaps.

## Context
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-risk-rule-enhancement/04-CONTEXT.md
@.planning/phases/04-risk-rule-enhancement/04-RESEARCH.md
@.planning/phases/04-risk-rule-enhancement/04-VALIDATION.md

## Tasks

<task id="04-01-01" type="docs">
  <name>Align docs with the real Phase 4 baseline</name>
  <files>README.md, docs/USER_MANUAL.md</files>
  <read_first>
    <file>README.md</file>
    <file>docs/USER_MANUAL.md</file>
    <file>src/risk/service.rs</file>
    <file>src/cli/handlers/risk.rs</file>
  </read_first>
  <action>Revise the risk sections so they explicitly describe the currently shipped baseline: `live_import`, `volatility-limit`, and `industry-blocklist` are real today; `industry-limit` is not yet a real runtime gate; `auto-reduce` remains deferred until its behavior is made explicit.</action>
  <acceptance_criteria>
    <item>`README.md` no longer blurs shipped risk behavior and deferred placeholder behavior.</item>
    <item>`docs/USER_MANUAL.md` explicitly tells operators which Phase 4 slices are already real today.</item>
    <item>No updated doc section claims `industry-limit` or `auto-reduce` are fully shipped if code still says otherwise.</item>
  </acceptance_criteria>
  <verify>rg -n "industry-limit|auto-reduce|live_import|volatility-limit|industry-blocklist" README.md docs/USER_MANUAL.md</verify>
  <done>Phase 4 starts from an explicit baseline rather than mixed operator assumptions.</done>
</task>

<task id="04-01-02" type="test">
  <name>Lock the current risk baseline in repo hygiene and CLI regressions</name>
  <files>tests/repo_hygiene_test.rs, src/cli/tests/risk.rs</files>
  <read_first>
    <file>tests/repo_hygiene_test.rs</file>
    <file>src/cli/tests/risk.rs</file>
    <file>README.md</file>
    <file>docs/USER_MANUAL.md</file>
  </read_first>
  <action>Add or update regressions so docs and risk CLI tests fail if the current baseline drifts: shipped live-import/volatility/industry-blocklist surfaces stay documented, while `industry-limit` placeholder status and deferred `auto-reduce` behavior stay explicit until later plans complete them.</action>
  <acceptance_criteria>
    <item>`tests/repo_hygiene_test.rs` locks the current Phase 4 baseline wording.</item>
    <item>`src/cli/tests/risk.rs` still reflects the currently exposed risk CLI surface without overstating deferred behavior.</item>
    <item>`env RUSTFLAGS=-Clink-arg=-fuse-ld=bfd cargo test --test repo_hygiene_test` exits 0.</item>
  </acceptance_criteria>
  <verify>env RUSTFLAGS=-Clink-arg=-fuse-ld=bfd cargo test --test repo_hygiene_test</verify>
  <done>The already-shipped risk slices are locked before deeper Phase 4 implementation begins.</done>
</task>

## Verification
- Run `env RUSTFLAGS=-Clink-arg=-fuse-ld=bfd cargo test --test repo_hygiene_test`.
- Re-run any updated risk CLI regressions.

## Success Criteria
- [ ] Current shipped risk behavior is documented accurately.
- [ ] Placeholder or deferred Phase 4 behavior stays explicitly bounded.
- [ ] Phase 4 can proceed from a stable baseline instead of mixed assumptions.
