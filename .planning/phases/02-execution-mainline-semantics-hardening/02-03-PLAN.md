---
phase: 02-execution-mainline-semantics-hardening
plan: 03
title: docs and cli boundary resync
type: execute
wave: 3
requires: [SEM-03, SEM-01]
requirements_addressed: [SEM-03, SEM-01]
depends_on: [02-01, 02-02]
files_modified:
  - README.md
  - docs/USER_MANUAL.md
  - tests/repo_hygiene_test.rs
  - src/cli/handlers/tests/mod.rs
autonomous: true
user_setup: []
must_haves:
  truths:
    - docs and CLI examples use the same semantics for request completion, order terminality, and mode boundaries
    - `paper`, `mock_live`, `qmt_live`, and generic `live` remain clearly separated
  artifacts:
    - path: README.md
      provides: final operator-facing execution semantics wording
    - path: docs/USER_MANUAL.md
      provides: final user-facing semantics and diagnostics guidance
    - path: tests/repo_hygiene_test.rs
      provides: repo-level wording locks
  key_links:
    - from: tests/repo_hygiene_test.rs
      to: README.md
      via: exact semantics wording locks
    - from: tests/repo_hygiene_test.rs
      to: docs/USER_MANUAL.md
      via: exact semantics wording locks
---

# Phase 2 Plan 03: Docs And CLI Boundary Resync

## Objective
- **What:** Re-sync docs and repo-level wording with the final Phase 2 CLI/operator semantics.
- **Why:** `SEM-03` is only complete when docs and operator output teach the same execution truth.
- **Output:** Updated docs and hygiene tests that lock the final semantics.

## Context
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-execution-mainline-semantics-hardening/02-CONTEXT.md
@.planning/phases/02-execution-mainline-semantics-hardening/02-RESEARCH.md
@.planning/phases/02-execution-mainline-semantics-hardening/02-VALIDATION.md

## Tasks

<task id="02-03-01" type="docs">
  <name>Update README and user manual to match final request semantics</name>
  <files>README.md, docs/USER_MANUAL.md</files>
  <read_first>
    <file>README.md</file>
    <file>docs/USER_MANUAL.md</file>
    <file>src/cli/handlers/mod.rs</file>
    <file>.planning/phases/02-execution-mainline-semantics-hardening/02-RESEARCH.md</file>
  </read_first>
  <action>Revise the request-list, request-show, and execution-daemon wording in README.md and docs/USER_MANUAL.md so they explicitly state: `request completed` means execution-path completion rather than order terminality; non-terminal `order_status` values remain possible after request completion; failed and canceled requests expose reason/timestamp diagnostics; and generic `live` is still different from the guarded `qmt_live` path. Keep the existing Phase 1 `mock_live` live-ready hardening wording intact unless the final Phase 2 formatter semantics require a strictly stronger phrase.</action>
  <acceptance_criteria>
    <item>`README.md` contains wording that distinguishes request completion from order terminality.</item>
    <item>`docs/USER_MANUAL.md` contains wording that distinguishes `qmt_live` from generic `live` and mentions request diagnostics.</item>
    <item>No updated doc section claims generic real broker `live` execution is already available.</item>
  </acceptance_criteria>
  <verify>rg -n "request completed|order_status|qmt_live|target_mode=live|failed_at|executed_at" README.md docs/USER_MANUAL.md</verify>
  <done>The primary user-facing docs teach the same semantics the CLI now renders.</done>
</task>

<task id="02-03-02" type="test">
  <name>Lock final semantics with repo hygiene and handler regressions</name>
  <files>tests/repo_hygiene_test.rs, src/cli/handlers/tests/mod.rs</files>
  <read_first>
    <file>tests/repo_hygiene_test.rs</file>
    <file>src/cli/handlers/tests/mod.rs</file>
    <file>README.md</file>
    <file>docs/USER_MANUAL.md</file>
  </read_first>
  <action>Add or update regression tests so repo_hygiene_test.rs and handler formatter tests fail if docs or CLI output drift away from the final Phase 2 semantics. Assert exact strings for request completion versus order terminality, for `failed_at`/`executed_at` style diagnostics when present, and for the `qmt_live` versus generic `live` capability boundary.</action>
  <acceptance_criteria>
    <item>`tests/repo_hygiene_test.rs` contains assertions for request-vs-order semantics and `qmt_live` / `live` boundary wording.</item>
    <item>`env RUSTFLAGS=-Clink-arg=-fuse-ld=bfd cargo test --test repo_hygiene_test` exits 0.</item>
    <item>Any added handler-formatter regression exits 0.</item>
  </acceptance_criteria>
  <verify>env RUSTFLAGS=-Clink-arg=-fuse-ld=bfd cargo test --test repo_hygiene_test</verify>
  <done>Phase 2 semantics are encoded in docs and tests rather than relying on operator memory.</done>
</task>

## Verification
- Run `env RUSTFLAGS=-Clink-arg=-fuse-ld=bfd cargo test --test repo_hygiene_test`.
- Re-run any added handler-formatter tests.
- Manually read the updated README and USER_MANUAL sections for request, daemon, and live-boundary semantics.

## Success Criteria
- [ ] Docs explicitly distinguish request completion from order terminality.
- [ ] Docs and tests explicitly preserve the `qmt_live` versus generic `live` boundary.
- [ ] Repo-level wording and CLI regressions lock the final Phase 2 semantics.
