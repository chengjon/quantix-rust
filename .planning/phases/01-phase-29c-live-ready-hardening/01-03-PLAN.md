---
phase: 01-phase-29c-live-ready-hardening
plan: 03
title: docs and operator truth sync
type: execute
wave: 3
requires: [EXE-02, EXE-03]
requirements_addressed: [EXE-02, EXE-03]
depends_on: [01-01, 01-02]
files_modified:
  - README.md
  - docs/USER_MANUAL.md
  - src/cli/handlers/tests/mod.rs
  - tests/repo_hygiene_test.rs
autonomous: true
user_setup: []
must_haves:
  truths:
    - user-facing docs and handler/regression tests describe mock_live as live-ready hardening, not real broker live execution
    - operator-facing outputs reflect non-final order states and reconciliation semantics
  artifacts:
    - path: README.md
      provides: updated execution boundary wording
    - path: docs/USER_MANUAL.md
      provides: updated operator/user guidance
    - path: src/cli/handlers/tests/mod.rs
      provides: handler-level regression coverage for output semantics
  key_links:
    - from: README.md
      to: docs/USER_MANUAL.md
      via: consistent mock_live/live wording
    - from: src/cli/handlers/tests/mod.rs
      to: README.md
      via: CLI/handler semantics must match docs
---

# Phase 1 Plan 03: Docs And Operator Truth Sync

## Objective
- **What:** Synchronize docs and operator-facing semantics with the hardened Phase 1 behavior.
- **Why:** The repo must not imply that real broker live execution is already available, and users need a clear explanation of non-final states and reconciliation scaffolding.
- **Output:** Updated docs plus regression tests that lock the intended boundary wording and status semantics.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-phase-29c-live-ready-hardening/01-CONTEXT.md
@.planning/phases/01-phase-29c-live-ready-hardening/01-RESEARCH.md
@.planning/phases/01-phase-29c-live-ready-hardening/01-VALIDATION.md

## Tasks

<task id="01-03-01" type="docs">
  <name>Update README and user manual to reflect Phase 1 boundaries</name>
  <files>README.md, docs/USER_MANUAL.md</files>
  <read_first>
    <file>README.md</file>
    <file>docs/USER_MANUAL.md</file>
    <file>.planning/ROADMAP.md</file>
    <file>docs/superpowers/specs/2026-03-22-phase29c-mock-live-execution-foundation-design.md</file>
  </read_first>
  <action>Revise the mock-live/live-ready sections in README.md and docs/USER_MANUAL.md so they explicitly mention non-final order states, delayed/partial fill and Unknown recovery expectations, and reconciliation scaffolding. State directly that real broker/live execution is still out of scope for this phase. Do not leave any wording that implies `mock_live` equals real live trading.</action>
  <acceptance_criteria>
    <item>`README.md` contains `mock_live` wording that mentions non-final states or recovery semantics.</item>
    <item>`docs/USER_MANUAL.md` contains wording that distinguishes `mock_live` from real `live` execution.</item>
    <item>No updated doc section claims broker-backed live order placement is available in Phase 1.</item>
  </acceptance_criteria>
  <verify>rg -n "mock_live|live-ready|non-final|Unknown|reconciliation" README.md docs/USER_MANUAL.md</verify>
  <done>The primary user-facing docs describe the exact Phase 1 capability boundary.</done>
</task>

<task id="01-03-02" type="test">
  <name>Lock operator-facing semantics with handler and hygiene regressions</name>
  <files>src/cli/handlers/tests/mod.rs, tests/repo_hygiene_test.rs</files>
  <read_first>
    <file>src/cli/handlers/tests/mod.rs</file>
    <file>tests/repo_hygiene_test.rs</file>
    <file>README.md</file>
    <file>docs/USER_MANUAL.md</file>
  </read_first>
  <action>Add or update regression tests in src/cli/handlers/tests/mod.rs and tests/repo_hygiene_test.rs so handler output, repo-level wording, or hygiene checks fail if mock-live/live terminology drifts. Assert concrete strings for non-final status visibility, reconciliation/live-gate wording, or the absence of claims that real broker live execution already exists.</action>
  <acceptance_criteria>
    <item>`src/cli/handlers/tests/mod.rs` or `tests/repo_hygiene_test.rs` contains assertions for `mock_live`/`live` boundary wording.</item>
    <item>`cargo test --test repo_hygiene_test` exits 0.</item>
    <item>Any targeted handler test introduced for this plan exits 0.</item>
  </acceptance_criteria>
  <verify>cargo test --test repo_hygiene_test</verify>
  <done>Operator/documentation semantics are encoded in regressions instead of relying on manual memory.</done>
</task>

## Verification
- Run `cargo test --test repo_hygiene_test`.
- Run any targeted handler test added in `src/cli/handlers/tests/mod.rs`.
- Manually read the updated README / USER_MANUAL sections to confirm they do not overclaim live capability.

## Success Criteria
- [ ] Docs explicitly describe mock-live non-final status and reconciliation semantics.
- [ ] Docs explicitly say real broker live execution is still out of scope.
- [ ] Regression tests lock the wording so future drift is visible.
