---
phase: 01-phase-29c-live-ready-hardening
plan: 01
title: mock-live lifecycle and fault hardening
type: execute
wave: 1
requires: [EXE-01, EXE-03]
requirements_addressed: [EXE-01, EXE-03]
depends_on: []
files_modified:
  - tests/mock_live_adapter_test.rs
  - tests/execution_kernel_test.rs
  - src/execution/mock_live.rs
  - src/execution/runtime_store.rs
  - src/execution/kernel.rs
autonomous: true
user_setup: []
must_haves:
  truths:
    - mock_live can express delayed fill, repeated partial fill, and Unknown/fault transitions without breaking existing paper behavior
    - runtime order lifecycle remains idempotent across repeated query and recovery calls
  artifacts:
    - path: tests/mock_live_adapter_test.rs
      provides: extended lifecycle/fault regression coverage
    - path: tests/execution_kernel_test.rs
      provides: kernel-level recovery and fill-delta hardening coverage
    - path: src/execution/mock_live.rs
      provides: hardened adapter/private-state transition logic
  key_links:
    - from: tests/mock_live_adapter_test.rs
      to: src/execution/mock_live.rs
      via: adapter lifecycle and private-state expectations
    - from: tests/execution_kernel_test.rs
      to: src/execution/kernel.rs
      via: recovery and fill-delta orchestration
---

# Phase 1 Plan 01: Mock-Live Lifecycle And Fault Hardening

## Objective
- **What:** Harden mock-live lifecycle behavior so delayed fills, repeated partial fills, Unknown recovery, and simulated transport faults are covered explicitly.
- **Why:** Phase 1 cannot claim live-ready hardening if lifecycle edge cases only work in narrow happy paths.
- **Output:** One hardened adapter/kernel slice with regression tests proving lifecycle and fault semantics.

## Context
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-phase-29c-live-ready-hardening/01-CONTEXT.md
@.planning/phases/01-phase-29c-live-ready-hardening/01-RESEARCH.md
@.planning/phases/01-phase-29c-live-ready-hardening/01-VALIDATION.md

## Tasks

<task id="01-01-01" type="test">
  <name>Expand adapter lifecycle and fault-matrix regressions</name>
  <files>tests/mock_live_adapter_test.rs</files>
  <read_first>
    <file>tests/mock_live_adapter_test.rs</file>
    <file>src/execution/mock_live.rs</file>
    <file>docs/superpowers/specs/2026-03-22-phase29c-mock-live-execution-foundation-design.md</file>
  </read_first>
  <action>Add focused tests in tests/mock_live_adapter_test.rs for: a repeated partial-fill plan with at least three fill steps; an Unknown -> accepted -> Unknown -> filled chain; a transient query-fault chain that preserves filled quantity and does not duplicate fill_details; and a cancel-after-partial-fill path that leaves already filled quantity intact. Reuse the existing FixedClock and buy_request helpers instead of creating new fixtures.</action>
  <acceptance_criteria>
    <item>`tests/mock_live_adapter_test.rs` contains test names covering `unknown`, `partial`, `fault`, and `cancel` scenarios.</item>
    <item>`cargo test --test mock_live_adapter_test` exits 0.</item>
    <item>No test removes current accepted-by-default, query progression, or duplicate fill-id coverage.</item>
  </acceptance_criteria>
  <verify>cargo test --test mock_live_adapter_test</verify>
  <done>The adapter test file proves the lifecycle/fault matrix expected by EXE-01 and EXE-03.</done>
</task>

<task id="01-01-02" type="code">
  <name>Harden mock-live adapter private-state transitions</name>
  <files>src/execution/mock_live.rs, src/execution/runtime_store.rs</files>
  <read_first>
    <file>src/execution/mock_live.rs</file>
    <file>src/execution/runtime_store.rs</file>
    <file>tests/mock_live_adapter_test.rs</file>
    <file>docs/superpowers/specs/2026-03-22-phase29c-mock-live-execution-foundation-design.md</file>
  </read_first>
  <action>Update src/execution/mock_live.rs so adapter-private state can represent the newly tested fault and lifecycle chains without duplicating fills or regressing Unknown semantics. If runtime-store helpers in src/execution/runtime_store.rs are missing fields or helper methods needed by the new adapter state, add the minimum store changes there rather than embedding ad hoc SQL in the adapter. Keep public order truth non-terminal when status is Unknown.</action>
  <acceptance_criteria>
    <item>`src/execution/mock_live.rs` contains explicit handling for repeated partial fills and Unknown/fault progression.</item>
    <item>`src/execution/runtime_store.rs` still exposes `mock_live_orders` support and any new helper is exercised by tests.</item>
    <item>`cargo test --test mock_live_adapter_test` exits 0 after the code changes.</item>
  </acceptance_criteria>
  <verify>cargo test --test mock_live_adapter_test</verify>
  <done>The adapter and store support the tested lifecycle matrix with no duplicated fill application.</done>
</task>

<task id="01-01-03" type="test+code">
  <name>Extend kernel recovery tests for lifecycle and fill-delta invariants</name>
  <files>tests/execution_kernel_test.rs, src/execution/kernel.rs</files>
  <read_first>
    <file>tests/execution_kernel_test.rs</file>
    <file>src/execution/kernel.rs</file>
    <file>src/execution/mock_live.rs</file>
    <file>src/execution/runtime_store.rs</file>
  </read_first>
  <action>Add or tighten tests in tests/execution_kernel_test.rs for repeated Unknown recovery, fault-driven query retries, and multi-step partial fills so the kernel proves: paper account mutates only on newly observed fill deltas; Unknown exhaustion does not change public order truth to a false terminal state; and repeated recovery does not double-apply the same fill id. Update src/execution/kernel.rs only where needed to satisfy those tests while preserving existing paper-mode semantics.</action>
  <acceptance_criteria>
    <item>`tests/execution_kernel_test.rs` contains coverage for repeated Unknown/fault recovery and multi-step fill-delta application.</item>
    <item>`cargo test --test execution_kernel_test` exits 0.</item>
    <item>The existing paper-mode tests in `tests/execution_kernel_test.rs` remain present and passing.</item>
  </acceptance_criteria>
  <verify>cargo test --test execution_kernel_test</verify>
  <done>The kernel enforces EXE-01 and EXE-03 lifecycle/accounting invariants under non-happy-path recovery.</done>
</task>

## Verification
- Run `cargo test --test mock_live_adapter_test --test execution_kernel_test`.
- Confirm the new tests cover delayed/partial/Unknown/fault scenarios rather than only happy paths.

## Success Criteria
- [ ] Mock-live lifecycle tests explicitly cover delayed, partial, Unknown, and simulated fault chains.
- [ ] Kernel recovery tests prove fill-delta idempotency and correct Unknown handling.
- [ ] No paper-mode regression is introduced while hardening mock-live behavior.
