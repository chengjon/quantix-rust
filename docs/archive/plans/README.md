# Archived Plans

This directory stores historical planning documents that are no longer canonical entrypoints.

- Keep active implementation plans in `docs/superpowers/plans/`.
- Keep old branch- or phase-specific planning snapshots here once execution has moved on.
- Preserve file history with `git mv` when relocating plan documents into this bucket.
