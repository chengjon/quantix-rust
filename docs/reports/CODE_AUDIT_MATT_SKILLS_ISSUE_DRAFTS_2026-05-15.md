# Code Audit Issue Drafts for Matt Pocock Skills

**Date:** 2026-05-15
**Source reports:**

- `docs/reports/CODE_AUDIT_FINAL_2026-05-15.md`
- `docs/reports/IMPECCABLE_AUDIT_CODE_AUDIT_EXECUTION_SPEC_2026-05-15.md`
- `docs/CODE_AUDIT_EVIDENCE/findings.csv`
- `docs/CODE_AUDIT_EVIDENCE/cargo-gates.md`

> 状态源说明：本文是 issue 发布前草案，不作为功能状态注册表。当前功能状态、已设计/待实现项、证据和边界，以根目录 `FUNCTION_TREE.md` 的状态注册表为准。

## Publishing Notes

- Default issue tracker: GitHub Issues for `chengjon/quantix`.
- Maintainer confirmed `chengjon/quantix` as the publication target.
- Publish in two batches:
  - Batch 1: gate-remediation issues 1-3.
  - Batch 2: audit-spec-hardening issues 4-9.
- Clippy warning policy selected for issue 8: warning-tolerant gate with required finding/non-issue evidence for warning classes above threshold. Do not switch to `-D warnings` as part of this issue.
- The maintainer approved publication on 2026-05-15.
- Published issues should use the label mapping in `docs/agents/triage-labels.md`.
- GitHub currently does not have the configured `type:*` and `status:*` labels. Issues were published without applying labels; each issue body records the suggested category and state.
- Each published issue or triage comment should start with:

```text
This was generated by AI during triage.
```

## Draft Issue Set

## Published Issue Map

| Local issue | GitHub issue | Batch | State | Title |
|---:|---|---:|---|---|
| 1 | `chengjon/quantix#1` | 1 | open | Close the formatting gate for factor scoring |
| 2 | `chengjon/quantix#2` | 1 | open | Diagnose and fix the factor score CSV output test failure |
| 3 | `chengjon/quantix#3` | 1 | closed | Make the release build gate reproducible |
| 4 | `chengjon/quantix#4` | 2 | closed | Harden the audit scan scope to exclude repo-local worktrees |
| 5 | `chengjon/quantix#5` | 2 | closed | Add Graphiti read-side evidence to the audit package |
| 6 | `chengjon/quantix#6` | 2 | closed | Define documentation hygiene gate commands |
| 7 | `chengjon/quantix#7` | 2 | closed | Clarify Phase 4 path coverage for persistence and notification review |
| 8 | `chengjon/quantix#8` | 2 | closed | Encode the clippy warning evidence policy |
| 9 | `chengjon/quantix#9` | 2 | closed | Define evidence archive and manifest behavior |

## Post-Review Merge Status

Post-review supplemental work from the parallel line was merged into this audit closure thread on 2026-05-18.

Closed as completed:

- `chengjon/quantix#4`
- `chengjon/quantix#5`
- `chengjon/quantix#6`
- `chengjon/quantix#7`
- `chengjon/quantix#8`
- `chengjon/quantix#9`

Still open in GitHub until commit/push evidence is synchronized:

- `chengjon/quantix#1`
- `chengjon/quantix#2`

The local runtime gate loop is closed after follow-up remediation, but #1 and #2 should remain open in GitHub until the source/evidence commit is available to the issue tracker and linked in issue comments.

## Runtime Gate Update

Runtime gate remediation continued on 2026-05-18:

- `chengjon/quantix#1` remains open in GitHub pending commit/push evidence. Locally, `cargo fmt --check` passes after formatting the factor scoring work.
- `chengjon/quantix#2` remains open in GitHub pending commit/push evidence. The failure was reproduced and locally fixed by extracting plain string values from Polars `AnyValue` before writing factor score CSV rows. Local verification passed for the targeted test, full `factor_pipeline_test`, `cargo fmt --check`, `cargo test --all-targets`, `cargo build --release --quiet`, and `repo_hygiene_test`. GitHub evidence comment before commit: `https://github.com/chengjon/quantix/issues/2#issuecomment-4471876127`.
- `chengjon/quantix#3` is closed. `cargo build --release` was reproduced as a pass, with the long-running build handling recorded at `docs/CODE_AUDIT_EVIDENCE/logs/cargo-build-release-20260517T174008Z.log`.

### 1. Close the formatting gate for factor scoring

**Type:** AFK
**Category:** `bug`
**Initial state:** `ready-for-agent`
**Blocked by:** none
**Source finding:** `AUDIT-S2-010`

#### What to build

Restore the formatting gate by applying the smallest rustfmt-only change needed for `src/factor/scoring.rs`.

#### Acceptance criteria

- [x] `cargo fmt --check` passes.
- [x] No logic changes are included for the formatting closure itself.
- [x] `docs/CODE_AUDIT_EVIDENCE/findings.csv` records `AUDIT-S2-010` closure with fresh gate evidence.

#### Agent brief notes

- Treat this as a closure-stage gate task.
- Do not touch unrelated dirty worktree changes.
- Do not expand into warning cleanup or refactoring.

### 2. Diagnose and fix the factor score CSV output test failure

**Type:** AFK, with escalation to HITL if the public output contract is ambiguous
**Category:** `bug`
**Initial state:** `ready-for-agent`
**Blocked by:** none
**Source finding:** `AUDIT-S2-011`

#### What to build

Reproduce `factor_score_cli_writes_csv_output`, determine whether the implementation or the test expectation owns the CSV contract, then make the minimal behavior-level fix.

#### Acceptance criteria

- [x] `cargo test --test factor_pipeline_test factor_score_cli_writes_csv_output` reproduced the failure before the fix.
- [x] The root cause is documented in the issue and evidence notes.
- [x] The fixed test verifies user-visible CSV output through the public path, not private implementation details.
- [x] `cargo test --test factor_pipeline_test factor_score_cli_writes_csv_output` passes after the fix.
- [x] `cargo test --all-targets` was rerun after this and the formatting gate were closed.

#### Agent brief notes

- Use `diagnose` first: reproduce, minimize, hypothesize, instrument only if needed, fix, regression-test.
- Use `tdd` for the fix once the contract is clear.
- If the correct CSV row ordering or value semantics require product judgment, move the issue to `ready-for-human`.

### 3. Make the release build gate reproducible

**Type:** AFK
**Category:** `bug`
**Initial state:** `ready-for-agent`
**Blocked by:** issue 1 and issue 2 for final release-confidence closure
**Source finding:** `AUDIT-S3-010`

#### What to build

Turn the current `cargo build --release` `NEEDS-REPRO` state into a reproducible pass or a reproducible failure with a clear blocker.

#### Acceptance criteria

- [ ] The command is run with a documented timeout budget and log capture path.
- [ ] If the build passes, the exact command and exit status are recorded.
- [ ] If the build fails or times out, the blocker, duration, process cleanup, and next action are recorded.
- [ ] No unmanaged release-build process is left running.

#### Agent brief notes

- Use `diagnose`, not speculative cleanup.
- Do not edit production code unless a concrete build failure points to a specific fix and the maintainer approves remediation.

### 4. Harden the audit scan scope to exclude repo-local worktrees

**Type:** AFK
**Category:** `enhancement`
**Initial state:** `ready-for-agent`
**Blocked by:** none
**Source finding:** Impeccable audit P1

#### What to build

Update the code-audit execution spec so Phase 2 uses explicit include and exclude path rules. The key exclusion is `.worktrees/**`.

#### Acceptance criteria

- [ ] The spec names included roots for Rust source, tests, benches, and relevant scripts.
- [ ] The spec excludes `.worktrees/**`, `target/**`, generated/vendor/cache paths, and any other non-current workspace paths.
- [ ] Phase 0 requires recording main-workspace file counts and excluded worktree counts.
- [ ] The final report template or completion criteria makes scan scope auditable.

#### Agent brief notes

- This is audit-spec hardening, not code remediation.
- Keep `FUNCTION_TREE.md` as the only feature status registry.

### 5. Add Graphiti read-side evidence to the audit package

**Type:** AFK
**Category:** `enhancement`
**Initial state:** `ready-for-agent`
**Blocked by:** none
**Source finding:** Impeccable audit P1

#### What to build

Add a required artifact or final-report section that records Graphiti reads used during audit execution.

#### Acceptance criteria

- [ ] The spec requires recording Graphiti query text, `group_id`, result summary, and fact UUIDs when facts influence conclusions.
- [ ] The spec records no-result outcomes.
- [ ] The spec records read failures and fallback behavior.
- [ ] The final Graphiti write-ingest requirement remains unchanged.

#### Agent brief notes

- Do not use Graphiti as code truth.
- Graphiti evidence should explain review memory influence only.

### 6. Define documentation hygiene gate commands

**Type:** AFK
**Category:** `enhancement`
**Initial state:** `ready-for-agent`
**Blocked by:** none
**Source finding:** Impeccable audit P2

#### What to build

Replace the vague completion criterion "passes repository documentation hygiene checks" with exact commands and artifact checks.

#### Acceptance criteria

- [ ] The spec lists exact documentation hygiene commands.
- [ ] The spec defines which generated audit files are checked.
- [ ] The spec includes a status-source note check for generated Markdown reports.
- [ ] The spec records pass/fail output in the evidence package or final report.

#### Agent brief notes

- Candidate checks include `git diff --check` and `cargo test --test repo_hygiene_test`.
- Keep the command list grounded in existing repo tests.

### 7. Clarify Phase 4 path coverage for persistence and notification review

**Type:** AFK
**Category:** `enhancement`
**Initial state:** `ready-for-agent`
**Blocked by:** none
**Source finding:** Impeccable audit P2

#### What to build

Replace ambiguous Phase 4 labels such as "persistence and migrations" and "notification paths" with exact path coverage.

#### Acceptance criteria

- [ ] The spec maps persistence review to concrete paths such as `src/db/`, `src/*/storage.rs`, and relevant initialization scripts.
- [ ] The spec maps notification review to concrete paths such as `src/monitoring/notification.rs`, `src/monitoring/notification/`, and `src/cli/handlers/notify.rs` when present.
- [ ] Each path can be marked `deep-reviewed`, `sampled`, `not present`, or `out of scope`.
- [ ] Residual risk for unreviewed paths is explicit.

#### Agent brief notes

- This is scope clarification only.
- Do not expand into implementation review or source edits.

### 8. Encode the clippy warning evidence policy

**Type:** AFK
**Category:** `enhancement`
**Initial state:** `ready-for-agent`
**Blocked by:** none
**Source finding:** Impeccable audit P2 and current clippy evidence

#### What to build

Document a warning-tolerant clippy policy for the audit gate. The gate may pass by exit status, but warning classes above a defined threshold must become findings or documented non-issues.

#### Acceptance criteria

- [ ] The spec keeps `cargo clippy --all-targets --all-features` as the gate command unless a future maintainer decision changes it.
- [ ] Warning-tolerant mode defines thresholds and required finding/non-issue evidence for warning classes.
- [ ] Clippy warning summaries record warning count, top warning classes, representative files, and classification outcome.
- [ ] The final report cannot claim warning-free quality when clippy passes with warnings.
- [ ] The chosen policy is documented in the audit execution spec.

#### Agent brief notes

- Current evidence records clippy as passing with 220 warning diagnostics.
- Do not convert the project to `-D warnings` in this issue.

### 9. Define evidence archive and manifest behavior

**Type:** AFK
**Category:** `enhancement`
**Initial state:** `ready-for-agent`
**Blocked by:** none
**Source finding:** Impeccable audit P2

#### What to build

Make audit evidence refresh behavior deterministic by defining an archive and manifest mechanism.

#### Acceptance criteria

- [ ] The spec defines where historical evidence is archived before refresh.
- [ ] The spec requires a manifest with old checksum, new checksum, command/source, timestamp, and reason for refresh.
- [ ] The final report references the manifest when evidence is refreshed.
- [ ] Existing evidence is not overwritten without enough reconstruction context.

#### Agent brief notes

- Keep this limited to evidence lifecycle documentation.
- Do not rewrite old evidence as part of this issue unless explicitly requested.

## Follow-Up Questions

1. Should the configured labels from `docs/agents/triage-labels.md` be created in `chengjon/quantix` and applied to issues #1-#9?
2. Should issue #1 and #2 be assigned immediately to an AFK implementation agent, or should gate remediation wait until labels are created?
