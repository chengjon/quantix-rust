task:
  id: "P0.8b-backfill"
  title: "OpenStock P0.8b Graphiti backfill"

scope:
  allowed_paths:
    - "docs/reports/OPENSTOCK_DATA_CONSUMPTION_P0_8B_GRAPHITI_BACKFILL_2026-06-26.md"
    - "README.md"
    - "CHANGELOG.md"
    - "FUNCTION_TREE.md"
    - ".governance/active-gates.json"
    - ".governance/active-gates.md"
    - ".governance/programs/project-governance/nodes.json"
    - ".governance/programs/project-governance/tree.md"
    - ".governance/programs/project-governance/cards/P0.8b-backfill.yaml"
  forbidden_paths:
    []

non_goals:
  - "No production Rust code, tests, fixtures, OpenStock parser behavior, CLI wiring, persistence, qmt_live, miniQMT, or unwrap cleanup changes"

acceptance:
  commit_gate:
    - "Docs/governance only; function-tree scope-check/validate; openspec validate --all --strict; git diff --check; GitNexus detect_changes before commit"
  closeout_gate:
    - "Backfill report records Graphiti episode UUID and stalled ingest state; PR CI and master CI pass"

evidence:
  current_head: "02568972ac442b98168d5614bfff1b9c947fdae2"
  notes: []
