task:
  id: "P0.8"
  title: "OpenStock data consumption OpenSpec"

scope:
  allowed_paths:
    - "openspec/changes/openstock-data-consumption-p0-8/proposal.md"
    - "openspec/changes/openstock-data-consumption-p0-8/design.md"
    - "openspec/changes/openstock-data-consumption-p0-8/tasks.md"
    - "openspec/changes/openstock-data-consumption-p0-8/specs/openstock-data-consumption/spec.md"
    - "docs/reports/OPENSTOCK_DATA_CONSUMPTION_P0_8_OPENSPEC_2026-06-26.md"
    - "FUNCTION_TREE.md"
    - "README.md"
    - "CHANGELOG.md"
    - ".governance/programs/project-governance/cards/P0.8.yaml"
    - ".governance/programs/project-governance/nodes.json"
    - ".governance/programs/project-governance/tree.md"
    - ".governance/active-gates.json"
    - ".governance/active-gates.md"
  forbidden_paths:
    []

non_goals:
  - "No production Rust code changes in this planning slice."
  - "No qmt_live runtime probing, canary, submit/query/cancel, bridge protocol, storage schema, ExecutionAdapter, or OrderStatus changes."
  - "No ClickHouse writes or external OpenStock runtime calls in this slice."
  - "No miniQMT registry or market-manifest behavior changes."

acceptance:
  commit_gate:
    - "openspec validate openstock-data-consumption-p0-8 --strict passes."
    - "openspec validate --all --strict passes or any unrelated pre-existing failure is documented."
    - "git diff --check passes."
    - "FUNCTION_TREE scope-check and validate pass."
    - "GitNexus detect_changes reports documentation/governance-only scope."
  closeout_gate:
    - "P0.8 node closed with active gates none."

evidence:
  current_head: "4c9c7a6edc203ffa36d666eab78e399714d95128"
  notes: []
