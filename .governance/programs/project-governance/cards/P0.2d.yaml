task:
  id: "P0.2d"
  title: "Execution storage namespace semantics"

scope:
  allowed_paths:
    - "src/execution/mode_semantics.rs"
    - "tests/execution_mode_semantics_test.rs"
    - "tests/repo_hygiene_test.rs"
    - "FUNCTION_TREE.md"
    - ".governance/**"
  forbidden_paths:
    []

non_goals:
  - "No change to CliRuntime::load default path behavior"
  - "No ExecutionAdapter trait or OrderStatus/schema changes"
  - "No qmt_live bridge/runtime behavior change"
  - "No paper_sim_lifecycle implementation or storage migration"
  - "No .unwrap() cleanup"

acceptance:
  commit_gate:
    - "cargo fmt --check"
    - "cargo clippy --all-targets --all-features -- -D warnings"
    - "cargo test --all-targets"
    - "git diff --check"
    - "GitNexus detect_changes LOW/0 unexpected processes"
    - "FUNCTION_TREE validate/status/scope-check"
  closeout_gate:
    - "FUNCTION_TREE active gates repaired to 0"
    - "PR CI passes before merge"
    - "post-merge master CI passes"
    - "Graphiti docs/handoff memories written or backfill noted"

evidence:
  current_head: "2c1f4eac5d8aa008b8228ea17306270fc8719dc1"
  notes: []
